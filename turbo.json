{
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**"],
      "env": [
        "BACKEND_URL",
        "SESSION_SECRET_KEY",
        "SUPABASE_URL",
        "SUPABASE_API_KEY"
      ]
    },
    "lint": {},
    "dev": {
      "cache": false
    }
  }
}
